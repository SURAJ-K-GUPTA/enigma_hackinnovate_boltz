<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Fontawesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <script
      src="https://kit.fontawesome.com/42a182743f.js"
      crossorigin="anonymous"
    ></script>
    <!-- CSS -->
    <link rel="stylesheet" href="/css/form.css" />
    <title>Update artist</title>
  </head>
  <body>
    <!-- nav -->
  <%-include('../partials/navbar.ejs') %>
    <div class="form-container">
      <!-- icon -->
      <h1>
        Update Artist form
        <i class="fas fa-artist-plus"></i>
      </h1>

      <%if(error){%>
        <small class="error">
          <i class="fas fa-exclamation-circle"></i>
          <%=error%>
        </small>
      <%}%>
      <form action="/api/v1/artists/update?_method=put" method="POST">
        <!-- full name -->
        <label for="fullname">Full Name</label>
        <input type="text" value="<%=artist?.fullname%>" name="fullname" id="name" />
        <!-- email -->
        <label for="email">Email</label>
        <input type="email" value="<%=artist?.email%>" name="email" id="email" />
        <!-- role -->
        <label for="role">Role</label>
        <input type="text" value="<%=artist?.role%>" name="role" />
        <!-- bio -->
        <label for="bio">Bio</label>
        <input type="text" value="<%=artist?.bio%>" name="bio" />


        <button type="submit">Update Artist</button>

      </form>
    </div>
    <!-- footer -->
    <%-include('../partials/footer.ejs') %>
  </body>
</html>
